# 기본은 모두 텍스트로 취급 + LF로 통일
* text=auto eol=lf

# 윈도우 전용 스크립트만 CRLF 허용 (필요 시)
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# 바이너리는 건드리지 않음
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.pkl binary












